<div class="container">
  <div class="row">
   <div class="col-md-12">
<h1>Items</h1>

      <table class="table genres-show">
        <th>商品名<br>商品画像</th>
        <th>ジャンル</th>
        <th>説明</th>
        <th>価格</th>
        <th>作成時刻</th>
        <th>更新日時</th>
        <th></th>
        <th></th>
        <% @item.each do |item| %>
        <tr>
          <td>
            <%= item.name %><br>

            <%= attachment_image_tag item, :image, :fill, 50, 50, fallback: "no_image.jpg"%>
                </td>


              <td><%= item.genre.name %></td>

              <td><%= item.introduction %></td>
              <td><%= item.price %></rd>
              <td>
                <%= item.created_at.strftime('%Y/%m/%d %H:%M') %>
                </td>
                <td><%= item.updated_at %></td>
              <td><%= link_to "show",  admin_item_path(item), class:"btn btn-info" %></td>
              <td><%= link_to "Edit",  edit_admin_item_path(item), class:"btn btn-success" %></td>

              <% end %>

            </div>
            

          </div>
            </div>
            <%= link_to "新規作成",  new_admin_item_path, class:"btn btn-primary" %>
        </div>
      


